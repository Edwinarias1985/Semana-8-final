# Semana 8 final
Bueno, como primera medida primero se analiza lo que pide el ejercicio para poder implementar e ir solucionando. Entonces primero se valida con el ciclo While, que se utiliza para que no avance hasta que se cumpla una condición que es en la nota de 0 y 5 por lo que, si el usuario digita otro valor entonces sería errado y se repetiría el ciclo solicitando nuevamente el dato.
Por otro lado se utilizó los operadores de tipo comparativo que es el (OR) || donde permite la evaluación de la nota si es menor a de 0 o mayor a 5, y así arroja el error de inmediato. Y bueno en la clase de la profe se ingresaba el (int) que es número entero, entonces se utiliza el double para poder ingresar decimales. Al igual se mantiene lo que aplicaba la profesora que, es al final el (Console.Readkey) para evitar ver toda la ruta de la carpeta, igual se mantuvo color de fondo y color de letra con el (Backgroundcolor y Foregroundcolor), como en los ejercicios de clase. 
Muchas gracias
